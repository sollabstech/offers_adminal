import type { Vendor, Product, RevenueData } from "@/types";

// No demo/seed data — all data is managed via the admin panel and Firestore.
export const vendors: Vendor[] = [];
export const products: Product[] = [];
export const revenueData: RevenueData[] = [];
